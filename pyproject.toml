[project]
name = "mcp-hello-world"
version = "0.1.4"
description = "Minimal MCP hello server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
mcp-hello-world = "mcp_hello.cli:main"
