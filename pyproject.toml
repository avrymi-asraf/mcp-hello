[project]
name = "mcp-hello"
version = "0.1.0"
description = "Minimal MCP hello server"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
mcp-hello = "mcp_hello.cli:main"
